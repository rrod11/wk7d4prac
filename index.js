// First Vowel
// Write a function firstVowel(str) that takes in a string and returns
// the first vowel that appears sequentially in the string. If the string does not
// contain a vowel, return null.

function firstVowel(str) {
    if()
}
///
